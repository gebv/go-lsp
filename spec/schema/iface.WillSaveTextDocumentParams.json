{
    "$id": "/WillSaveTextDocumentParams",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "definitions": {
        "DocumentUri": {
            "description": "A tagging type for string properties that are actually document URIs.",
            "type": "string"
        },
        "TextDocumentIdentifier": {
            "description": "A literal to identify a text document in the client.\nThe TextDocumentIdentifier namespace provides helper functions to work with\n[TextDocumentIdentifier](#TextDocumentIdentifier) literals.",
            "properties": {
                "uri": {
                    "$ref": "/WillSaveTextDocumentParams#/definitions/DocumentUri",
                    "description": "The text document's uri."
                }
            },
            "type": "object"
        },
        "TextDocumentSaveReason": {
            "description": "Represents reasons why a text document is saved.",
            "enum": [
                1,
                2,
                3
            ],
            "type": "number"
        }
    },
    "description": "The parameters send in a will save text document notification.",
    "properties": {
        "reason": {
            "$ref": "/WillSaveTextDocumentParams#/definitions/TextDocumentSaveReason",
            "description": "The 'TextDocumentSaveReason'."
        },
        "textDocument": {
            "$ref": "/WillSaveTextDocumentParams#/definitions/TextDocumentIdentifier",
            "description": "The document that will be saved."
        }
    },
    "type": "object"
}

